{
  "name": "cms-ui",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^0.27.2",
    "dotenv": "^16.0.1",
    "node": "^18.7.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^5.2.0",
    "react-scripts": "5.0.1",
    "runtime-env-cra": "^0.2.4",
    "sass-loader": "^13.0.2"
  },
  "scripts": {
    "start": "NODE_ENV=docker API_HOST=http://localhost:8000 runtime-env-cra --config-name=./public/runtime-env.js && react-scripts start",
    "dev": "NODE_ENV=dev API_HOST=http://localhost:8000 runtime-env-cra --config-name=./public/runtime-env.js && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "node-sass": "^7.0.1"
  }
}
